# Copy to .env if you want to override defaults.
PORT=3000
# Where the SQLite file lives. On Vercel this is forced to /tmp automatically.
DB_PATH=./data/aniv.sqlite
# Set to 1 to re-seed content/*.json into the DB on every boot (handy while editing content).
RESEED=0
